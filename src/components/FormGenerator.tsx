import { useForm } from "../hooks/useForm"

export default function FormGenerator<T>() {
  

  const { values, errors, handleChange, handleSubmit, isSubmitting } = useForm<T>({
    
  })


  return (

  )
}
